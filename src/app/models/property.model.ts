export interface EmergencyNumbers {
  number: string,
  name: string,
  icon: string
}
export interface Property {
  id: string,
  direction: string,
  commune: string,
  city: string,
  region: string,
  number: string
}